<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'My App' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">MyApp</a>
      <div>
        <% if (user) { %>
          <span class="navbar-text me-3">Olá, <%= user %></span>
          <a href="/logout" class="btn btn-sm btn-outline-light">Deslogar</a>
        <% } else { %>
          <a href="/login" class="btn btn-sm btn-outline-light me-2">Logar</a>
          <a href="/register" class="btn btn-sm btn-outline-light">Registro</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <%- body %>
  </div>
</body>
</html>
